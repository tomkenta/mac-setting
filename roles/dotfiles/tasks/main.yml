- block:

  - name: checkout dotfiles
    git:
      repo: https://github.com/tomkenta/dotfiles.git
      dest: ~/work/settings/dotfiles

  - name: create symlinks
    file:
      src: ~/work/settings/dotfiles/{{ item.name }}
      dest: ~/{{ item.name }}
      state: link
      force: yes
    with_items: '{{ dot_files }}'

  - name: create symlink for karabiner
    file:
      src: ~/work/settings/dotfiles/.config/karabiner/karabiner.json
      dest: ~/.config/karabiner/karabiner.json
      state: link
      force: yes


  tags:
    - dotfiles
